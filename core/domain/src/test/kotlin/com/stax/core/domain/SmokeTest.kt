package com.stax.core.domain

import org.junit.jupiter.api.Test

/** Smoke test — verifies the JUnit 5 harness is wired correctly. */
class SmokeTest {

    @Test
    fun `junit5 harness runs`() = Unit
}
