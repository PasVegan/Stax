# Dashboard Presentation

Purpose: owns dashboard ViewModels, state, events, and Compose screens.

Allowed dependencies: `:core:domain`, `:core:presentation`, `:core:design-system`.

Key types: none yet.
