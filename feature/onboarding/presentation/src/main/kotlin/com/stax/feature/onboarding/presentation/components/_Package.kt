/**
 * Composables shared by the onboarding steps and the permission gates (§4.14, §4.15): the hero blob
 * illustration and the step-indicator pill row.
 *
 * These are onboarding-local building blocks, not design-system components — they carry product
 * meaning that only this flow has. They take primitives only and hold no state.
 */
package com.stax.feature.onboarding.presentation.components
