/**
 * Protocol Detail (§4.8).
 *
 * MVI screen: `ProtocolDetailViewModel` + `ProtocolDetailState/Action/Event`, the
 * `ProtocolDetailRoot`/`ProtocolDetailScreen` pair, and the cards of `ProtocolDetailSections`.
 *
 * Entry point: `ProtocolDetailRoot`, hosted by `protocolsEntries` as the detail pane of the
 * Protocols list-detail Scene (§6.4.2).
 */
package com.stax.feature.protocols.presentation.detail
