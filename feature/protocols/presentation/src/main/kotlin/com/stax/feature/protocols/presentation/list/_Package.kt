/**
 * Protocols list (§4.7).
 *
 * MVI screen: `ProtocolsListViewModel` + `ProtocolsListState/Action/Event`, the
 * `ProtocolsListRoot`/`ProtocolsListScreen` pair, and the `ProtocolCard` a row renders as.
 *
 * Entry point: `ProtocolsListRoot`, hosted by `protocolsEntries` as the list pane of the Protocols
 * list-detail Scene (§6.4.2).
 */
package com.stax.feature.protocols.presentation.list
